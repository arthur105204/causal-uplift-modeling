{
  "schema_version": "t10-causal-forest-config-v1",
  "task": "T10",
  "issue": 11,
  "input": {
    "selector": "configs/data_manifest.json",
    "split": {
      "config": "configs/t05_split.json",
      "required_lifecycle_state": "T05_SPLIT_ACCEPTED"
    },
    "preprocessing": {
      "config": "configs/t04_preprocessing.json",
      "required_lifecycle_state": "T04_ACCEPTED"
    },
    "governing_adrs": [
      "docs/adr/ADR-CF-implementation.md",
      "docs/adr/ADR-CF-bridge.md"
    ],
    "governing_decisions": ["D10", "D15", "D22", "D23 (superseded by D30 2026-08-18)", "D30", "D31 (locked -- one-shot CF compute policy: T11 scope, 500K seed-robustness, Stage-4 CF exception)", "D34 (locked -- CausalForest categorical representation: frequency-capped top-K + OTHER one-hot, K ladder 32 -> 16 -> 8, resource-only selection)"]
  },
  "decision_record": {
    "question": "Which Python Causal Forest implementation, exact config, and D30 scale plan satisfy ADR-CF-implementation's selection criteria, for T10's local preflight + Kaggle SMOKE/RESOURCE verification (no held-out, no validation, no real-data SMOKE executed yet)?",
    "candidates_compared": [
      "econml.grf.CausalForest (py-why/Microsoft, actively maintained, v0.17.0)",
      "econml.dml.CausalForestDML (same package, adds a DML nuisance-residualization pre-stage)",
      "causalml.inference.tree.CausalRandomForestRegressor (Uber, active, honesty lineage unverified against Wager-Athey/GRF)",
      "skgrf.GRFForestCausalRegressor (crflynn, wraps the literal grf C++ core, but low-activity: 34 stars, 79 commits, vendored grf core lagging current CRAN release)"
    ],
    "selected_candidate": "econml.grf.CausalForest",
    "selection_rationale": "Single-stage: fits directly on (X, T, y) by solving the local moment equation E[(Y - <theta(x),T> - beta(x))(T;1) | X=x]=0 -- theta(x) (treatment effect) and beta(x) (local intercept) are estimated jointly and locally via forest weights. No upfront nuisance-model fit, no cross-fitting stage -- verified directly from the installed source docstring (econml 0.17.0), not assumed from external documentation. This matches the project's existing pattern (T=design-assigned ITT; no covariate-varying propensity model fit anywhere in T07-T09) and avoids introducing a second nuisance-governance mechanism that CausalForestDML's W/nuisance stage would require. Best-maintained option (4.8k stars, active 2026 commits) versus skgrf's low-activity/version-lag risk and causalml's unverified forest-honesty lineage.",
    "not_selected_reason": {
      "econml.dml.CausalForestDML": "Adds a DML nuisance-residualization pre-stage (separate model_y/model_t fits) this project has no documented need for -- T is design-assigned, not a confounded observational treatment. Would introduce a second, larger nuisance-governance surface alongside X-Learner's OOF mechanism without justification. Retained as documented fallback only if econml.grf.CausalForest fails a later gate.",
      "causalml": "Forest-based CausalRandomForestRegressor's honesty/sample-splitting semantics are not confirmed equivalent to Wager-Athey GRF from public documentation; the package's own UpliftRandomForestClassifier (a different class in the same package) uses KL-divergence/ED/chi-square splitting and is explicitly not honest/GRF-style -- same-package name-similarity risk the ADR explicitly warns against.",
      "skgrf": "Small (34 stars, 79 commits), indeterminate recency, vendored grf C++ core reported behind current upstream (2.1.0 vs CRAN 2.6.1) -- highest integration/maintenance risk despite highest literal Wager-Athey fidelity."
    },
    "criterion_choice": "mse (package default). Verified from the installed econml 0.17.0 source docstring: 'mse' is the standard local-moment-estimation split rule (the reference GRF gradient-tree objective); 'het' is a distinct, non-default heterogeneity-score approximation. Frozen as the untouched default per CODE PLAN direction -- not selected by a fidelity ranking claim beyond what the source itself documents, and not tuned.",
    "inference_false_rationale": "This project's frozen uncertainty mechanism is the 500-draw paired arm-stratified validation bootstrap (AGENTS.md frozen foundation; docs/06), applied post hoc to already-computed scores -- estimator-agnostic, and never consumes any estimator's internal per-row analytic/BLB interval. inference=True's Bootstrap-of-Little-Bags machinery has no downstream consumer in this project's established pipeline; disabling it is the smaller, justified final configuration rather than leaving a default-True flag enabled with no user.",
    "n_jobs_determinism_finding": "Empirically verified during T10 IMPLEMENT (not assumed): with an identical random_state, n_jobs=2 and n_jobs=-1 each produced different predict() output than n_jobs=1 on the same data. n_jobs=1 is required for bit-identical reproducibility and is frozen despite its runtime cost; this is a correctness requirement, not a performance preference.",
    "n_estimators_constraint_finding": "econml.grf.CausalForest.fit() raises ValueError unless n_estimators is exactly divisible by subforest_size (default 4) -- discovered via direct source-level testing, not documented prominently. n_estimators=100 (package default) satisfies this (100/4=25) and is frozen unchanged across SMOKE/RESOURCE/FULL per CODE PLAN direction (no borrowed R-grf tree count, no per-stage tuning).",
    "serialization_finding": "Verified via direct pickle round-trip on a synthetic fixture: predictions after pickle.loads(pickle.dumps(model)) match the original exactly (bit-identical), confirming pickle as the serialization/reload mechanism for T10/T11 artifacts."
  },
  "frozen_config": {
    "module": "src.causal_forest_baseline",
    "config_hash_function": "src.causal_forest_baseline.config_hash",
    "econml_version": "0.17.0",
    "n_estimators": 100,
    "criterion": "mse",
    "honest": true,
    "inference": false,
    "min_samples_leaf": 5,
    "max_samples": 0.45,
    "min_balancedness_tol": 0.45,
    "subforest_size": 4,
    "max_depth": null,
    "n_jobs": 1,
    "random_state_by_stage": {"primary": 42, "robustness": [123, 2026]},
    "same_n_estimators_across_all_stages": true,
    "min_balancedness_tol_semantics_note": "Bounds split-size imbalance between a split's two children -- NOT a treatment/control arm-balance mechanism. Arm support is verified separately via honest_leaf_arm_support()."
  },
  "honesty_support_algorithm": {
    "module": "src.causal_forest_baseline",
    "function": "honest_leaf_arm_support",
    "role": "DIAGNOSTIC ONLY, not a pass/fail gate (corrected -- see support_gate_correction below). Reports per-(tree,leaf) treated/control counts.",
    "corrected_index_mapping": "tree.get_train_test_split_inds() returns indices LOCAL to forest.get_subsample_inds()[i], not global row indices -- verified empirically (max local index < len(subsample)). Global indices are subsample[split_local] / subsample[est_local]. Leaf support is computed from est_global only via tree.apply(X[est_global]), never split_global.",
    "diagnostic_evidence": "full empirical distribution (min / 1st percentile / median) of min(n_treated, n_control) across all (tree, leaf) pairs, reported but never gating"
  },
  "support_gate_correction": {
    "date": "post-SMOKE-attempt review",
    "original_gate_removed": "100% of (tree, leaf) pairs reachable at predict() time must have n_treated>=1 AND n_control>=1 -- REMOVED. Verified over-constrained by direct inspection of econml/grf/_base_grf.py: identification is a forest-AGGREGATE property (predict_alpha_and_jac() averages each tree's local moment contribution across all trees; theta(x) = pinv(aggregate_jac(x)) @ aggregate_alpha(x)), not a per-tree-per-leaf requirement. econml itself implements no such per-leaf constraint by default.",
    "finding_that_triggered_review": "First real-data local SMOKE attempt (200,000 train-only rows, seed 42, frozen config) found 32.7% of 615,156 reachable (tree,leaf) pairs degenerate (missing one arm, almost entirely zero-control) -- all 100/100 trees affected, median n_control per leaf = 1. Mathematically expected under ~85/15 treatment imbalance with an absolute min_samples_leaf=5 floor and unconstrained max_depth: P(all-treated leaf of size L) ~= 0.85^L (~44% at L=5), independent of N. This finding is retained as diagnostic evidence, not relabeled as estimator failure.",
    "new_gate": {
      "module": "src.causal_forest_baseline",
      "function": "aggregate_jacobian_support",
      "mechanism": "econml.grf.CausalForest.predict_alpha_and_jac(X) returns the aggregate alpha(x)/jac(x) econml itself uses to compute theta(x). Hard gate per query: alpha(x) finite, jac(x) finite, jac(x) has numpy.linalg.matrix_rank(jac(x)) == full parameter dimension (2: theta(T) + intercept beta) using matrix_rank's own default tolerance -- no invented condition-number cutoff -- and the resulting tau(x) prediction is finite.",
      "diagnostic_only_evidence": "condition-number distribution (min/p50/p95/p99/max) of jac(x) across all queries, reported but never gating"
    },
    "unchanged_frozen_parameters": ["min_samples_leaf", "min_var_fraction_leaf", "min_var_leaf_on_val", "estimator/class (econml.grf.CausalForest)", "n_estimators", "criterion", "honest", "inference", "max_samples", "min_balancedness_tol", "subforest_size", "max_depth", "n_jobs", "random_state policy"],
    "local_verification": "tests/test_causal_forest_baseline.py: test_aggregate_jacobian_support_passes_on_balanced_fixture, test_aggregate_jacobian_support_passes_despite_per_leaf_degeneracy (reproduces the ~85/15-imbalance per-leaf degeneracy synthetically and confirms the aggregate gate still passes), test_aggregate_jacobian_support_no_invented_condition_number_cutoff -- all passed.",
    "final_disposition_locked_D31": {
      "hard_correctness": ["alpha(x) finite for every query", "jac(x) finite for every query", "tau(x) finite for every query", "tau(x) non-degenerate (not constant, std>0)", "data/leakage/alignment/config correctness (row identity, feature contract, held-out isolation, config-hash match)"],
      "diagnostic_only_never_gating": ["aggregate Jacobian numerical rank", "aggregate Jacobian condition number", "per-leaf treatment/control support (honest_leaf_arm_support)"],
      "real_data_finding_at_2M_RESOURCE_scale": "t10_resource_20260819T093301Z_574711 (2,000,000 train-only rows, seed 42): alpha_all_finite=True, jac_all_finite=True, tau_all_finite=True (hard gate satisfied); jac_full_rank_fraction=0.999997 (6 of 2,000,000 queries rank-deficient, one exactly singular, max condition number=inf) -- correctly diagnostic-only under this disposition, not a failure. Confirmed via direct econml/grf/_base_grf.py source inspection: predict_alpha_and_jac()->pinv(jac) has zero internal rank/singularity handling of its own; EconML predicts through singular Jacobians by design via Moore-Penrose pinv.",
      "run_disposition": "t10_resource_20260819T093301Z_574711 is retained UNCHANGED as T10 RESOURCE correctness/resource evidence only. NOT rerun. NOT reused as the seed-42 leg of the D31 500,000-row seed-robustness table (no serialized model or common-validation prediction artifact was produced before the run's since-superseded old gate halted it, so it is not comparable to fresh seed-123/2026 validation-scored runs at the D31-mandated 500,000-row scale)."
    }
  },
  "synthetic_gate": {
    "module": "tests/test_causal_forest_baseline.py",
    "n_rows": 4000,
    "x_distribution": "iid N(0, I_12), matching FEATURE_COLUMNS f0-f11",
    "treatment_distribution": "Bernoulli(0.5), balanced (arm-imbalance-specific behavior is a real-data SMOKE/RESOURCE concern, not duplicated here)",
    "noise": "N(0, 1.0)",
    "fixtures": {
      "positive": {"true_effect": 2.0, "pass": "abs(mean(tau_hat) - 2.0) <= 0.20 AND P(tau_hat>0) >= 0.95"},
      "negative": {"true_effect": -2.0, "pass": "abs(mean(tau_hat) - (-2.0)) <= 0.20 AND P(tau_hat<0) >= 0.95"},
      "zero": {"true_effect": 0.0, "pass": "abs(mean(tau_hat)) <= 0.10"},
      "heterogeneous": {"true_effect": "2.0 * 1[f0>0]", "pass": "mean(tau_hat|f0>0) - mean(tau_hat|f0<=0) >= 1.0 AND spearman(tau_hat, 1[f0>0]) >= 0.5"}
    },
    "status": "all 4 fixtures passed (tests/test_causal_forest_baseline.py, 19/19 passed post-support-gate-correction)"
  },
  "scale_gating": {
    "policy": "D30",
    "stages": "SMOKE -> RESOURCE (exactly 1 gate) -> FULL",
    "smoke_size": 50000,
    "smoke_size_revision_note": "Revised from 200,000 to 50,000 (post-support-gate-correction patch). The prior local 200,000-row diagnostic run (t10_smoke_20260819T074012Z_483118, incomplete/failed under the old per-leaf gate) is retained as evidence but is NOT authoritative T10 SMOKE evidence -- it predates both the corrected gate and the corrected scale contract.",
    "resource_gate_count": 1,
    "resource_gate_size": 2000000,
    "full_size": 9785714,
    "no_second_gate_after_observing_results": true,
    "failure_disposition": "STOP for review -- not an automatic second gate, not a silent retry",
    "promotion_criteria": "correctness/support(aggregate-jacobian)/determinism/reload checks pass; resource evidence (wall time, peak RSS) recorded with no OOM/termination. No promotion criterion is a performance/effect metric -- training rows only, no ranking to select on.",
    "smoke_status": "SMOKE accepted: run_id t10_smoke_20260819T091713Z_996970, status COMPLETED_T10_SMOKE_VERIFIED, all hard gates passed.",
    "resource_status": "RESOURCE executed once: run_id t10_resource_20260819T093301Z_574711. Fit and predict on the complete 2,000,000-row cohort succeeded; hard correctness (alpha/jac/tau finite) held. Retained as correctness/resource evidence -- see decision_record.final_disposition_locked_D31. NO rerun performed or authorized. The RESOURCE stage is closed for T10's purposes."
  },
  "execution_environment": {
    "smoke_resource_benchmark_authoritative_environment": "KAGGLE (owner directive, 2026-08-24, superseding the prior LOCAL-authoritative override below for the D34 RESOURCE K-ladder specifically). Local remains authoritative for development, unit tests, and the tiny synthetic-fixture SMOKE exercised by tests/test_causal_forest_runner.py -- never for a real 2,000,000-row (or larger) fit.",
    "d34_resource_k_ladder_environment": "KAGGLE -- notebooks/internal/t10_causal_forest_resource.ipynb (K=32; separate future notebook/run_id for K=16/K=8 only if K=32's resource_gate_verdict fails). This notebook calls the identical src.causal_forest_baseline/src.causal_forest_runner code path scripts/t11_run_stage.py's CLI form also exercises -- no separate Kaggle-only implementation exists.",
    "pre_d34_local_environment_scope_HISTORICAL": "SUPERSEDED for the D34 RESOURCE gate, retained as historical record: LOCAL was authoritative for the pre-D32 T10 SMOKE/RESOURCE runs (owner directive, superseding the earlier Kaggle-authoritative directive and the stale wording still present in GitHub Issue #11, which this repository has no tool access to edit directly -- flagged for a minimal manual Issue amendment, not blocking execution). Those runs -- src/causal_forest_baseline.py, tests/test_causal_forest_baseline.py, t10_smoke_20260819T091713Z_996970, t10_resource_20260819T093301Z_574711 -- are STALE_FEATURE_REPRESENTATION per the D32 reopening event below and are not reused to authorize the D34 K-ladder gate.",
    "audit_environment_json_must_record": "actual execution-machine (Kaggle session) hardware/package specs at RESOURCE execution time, exactly as already done for the accepted pre-D32 local SMOKE run."
  },
  "seed_robustness_plan_D31": {
    "status": "PLANNED, NOT YET EXECUTED as of this config version",
    "population": "500,000 TRAIN rows, deterministic joint-(T,Y)-stratified, sampling seed 42, IDENTICAL cohort for all three model seeds",
    "model_seeds": [42, 123, 2026],
    "config": "identical frozen config across all three (see frozen_config above) -- only random_state differs",
    "scoring": "complete frozen VALIDATION cohort (2,096,938 rows), identical for all three seeds",
    "rule": "report all three seeds; never select a favorable seed; no held-out access",
    "rationale_for_500K_not_2M": "50K SMOKE has only 15 control conversions (correctness-oriented, not rare-outcome-robust); 500,000 rows provides materially stronger rare-outcome support for a seed-sensitivity comparison while remaining computationally bounded and clearly distinct from both SMOKE (correctness) and the one authorized FULL-scale fit (T11).",
    "does_not_replace": "T11's single authoritative FULL-scale (9,785,714-row) fit -- this plan estimates algorithmic seed sensitivity only."
  },
  "t11_full_and_stage4_policy_D31": {
    "status": "LOCKED (D31), NOT YET IMPLEMENTED -- pointer only; the authoritative T11 config/contract is created at T11 CODE PLAN",
    "t11_full_scope": "Exactly ONE FULL-scale Causal Forest fit through T18: complete frozen T05 TRAIN (9,785,714 rows), primary seed 42, no validation fitting, held-out sealed.",
    "stage4_exception": "Causal Forest is exempted from docs/06 Stage 4's combined-train+validation refit pattern (amended, D31). The exact serialized T11 FULL train-only seed-42 model becomes the T16-frozen CF artifact -- no additional refit. This exception applies ONLY to Causal Forest; Response/T-Learner/X-Learner Stage-4 rules are unchanged.",
    "t17_scope": "T17 scores that exact frozen model on held-out only. No CF training of any kind occurs at or after T17.",
    "only_governed_reason_to_repeat_training": "A genuine, demonstrated software or methodology defect discovered before held-out access (matching this project's existing Class-A correction precedent). Unfavorable-but-valid model performance is explicitly NOT a defect and never triggers a refit."
  },
  "bridge_decision": {
    "status": "DEFERRED (D22, ADR-CF-bridge.md, unchanged)",
    "activation_condition_met": false,
    "note": "econml.grf.CausalForest is a qualifying Python implementation satisfying the reviewed ADR criteria; no required correctness/honesty/support/inference capability has been found unavailable from it. No cross-language bridge is proposed."
  },
  "scope_guards": {
    "fit_on_validation_or_held_out": false,
    "access_held_out_labels_or_features": false,
    "run_real_data_smoke_this_turn": false,
    "run_resource_gate_this_turn": false,
    "rerun_2M_resource": false,
    "run_seed_robustness_this_turn": false,
    "run_t11_full_this_turn": false,
    "start_t11_implementation": false,
    "commit_or_push": false
  },
  "categorical_representation_D34": {
    "status": "SELECTED_PENDING_RESOURCE_GATE",
    "decision_record": "docs/decision_register.csv D34; docs/adr/ADR-CF-implementation.md Feature-representation status",
    "encoder": "src.causal_forest_baseline.CausalForestCategoricalEncoder",
    "encoding": "frequency-capped top-K + explicit OTHER one-hot, TRAIN-only fit and frozen, reused unchanged on validation/held-out",
    "k_ladder": [32, 16, 8],
    "k_selection_rule": "largest K in k_ladder whose RESOURCE-gate attempt passes; K=32 attempted first, K=16/K=8 only after a recorded prior failure; never selected by Qini/AUUC/uplift/conversion/model-performance metrics or by OTHER-bucket mass",
    "k_ladder_stop_rule": "if K=8 fails the RESOURCE gate, STOP for owner scope review -- no fourth rung, no substitute encoding",
    "tie_break_rule": "category selection ranked by (TRAIN frequency count DESC, category value ASC); frozen K sorted ascending by value for column order",
    "missing_value_policy": "FAIL CLOSED -- CausalForestCategoricalEncoder.fit()/transform() raise CausalForestEncodingError on any NaN in a categorical feature; no MISSING bucket, no routing to OTHER (owner decision 2026-08-24)",
    "resource_gate_population": "the already-predeclared T10 RESOURCE cohort (resource_gate_size below, 2,000,000 rows) -- no new population size introduced",
    "resource_gate_pass_criteria": {
      "peak_memory": "PROJECTED_FULL peak_rss_bytes <= 80% of an assumed 30 GB Kaggle CPU RAM environment (24,000,000,000 bytes)",
      "wall_clock": "PROJECTED_FULL total_projected_wall_seconds <= 10 hours (36,000 seconds)",
      "correctness": "all existing hard-correctness gates (alpha/jac/tau finite) and the serialized-model reload-equality check must pass",
      "swap_instability": "no swap-usage growth above baseline during the run",
      "never_gated_on": ["other_bucket_mass_by_feature", "qini", "auuc", "conversion_rate", "uplift_metric"]
    },
    "measured_vs_projected": "src.causal_forest_runner.project_full_scale_resource()/resource_gate_pass_fail() keep MEASURED_RESOURCE (this run's actual figures) and PROJECTED_FULL (conservative linear row-count-ratio extrapolation to the 9,785,714-row FULL scale) explicitly separate in resource_evidence.json",
    "not_yet_executed": "no RESOURCE-gate attempt at any K has been run as of this config version -- see execution_environment/scope_guards below",
    "resource_k32_kaggle_invocation": {
      "status": "PREPARED_NOT_EXECUTED",
      "notebook": "notebooks/internal/t10_causal_forest_resource.ipynb",
      "equivalent_cli": "scripts/t11_run_stage.py --stage resource --categorical-k 32 --population-size 2000000 --sampling-seed 42 --model-seed 42 --diagnostic-sample-size 100000 --run-id <t10_resource_k32_UTCSTAMP_MICROS> --i-understand-this-executes-real-data-model-training",
      "population_size": 2000000,
      "categorical_k": 32,
      "sampling_seed": 42,
      "model_seed": 42,
      "diagnostic_sample_size": 100000,
      "validation_scoring": "complete frozen VALIDATION cohort (2,096,938 rows), not bounded -- required for the PROJECTED_FULL 'VALIDATION-side already at full size' assumption to hold",
      "notebook_confirmation_gate": "I_UNDERSTAND_THIS_RUNS_REAL_COMPUTE boolean cell, mirroring the CLI's --i-understand-this-executes-real-data-model-training flag -- defaults False",
      "authorized_next_step_if_verdict_fails": "a separate, explicitly authorized categorical_k=16 attempt with a fresh run_id -- never automatic, never same run_id"
    }
  },
  "lifecycle_state": "T10_REPRESENTATION_SELECTED_PENDING_RESOURCE_GATE",
  "feature_semantics_reopening_event": {
    "date": "2026-08-24",
    "decision": "D32",
    "reason": "T10_ACCEPTED (below, preserved as superseded_lifecycle_state) was granted while fit_causal_forest() accepted the raw twelve-column float64 X.to_numpy() frame directly -- treating f1/f3/f4/f5/f6/f8/f9/f11 (categorical tokens per the publisher and the official Criteo benchmark) as ordered continuous quantities. src/causal_forest_baseline.py now fails closed (CausalForestRepresentationError) on that raw representation. econml.grf.CausalForest has no native categorical representation equivalent to LightGBM's, so an explicit CausalForest-specific encoding is required and has not been selected -- see ADR-CF-implementation.md's new Feature-representation gate/status.",
    "effect_on_historical_evidence": "accepted_smoke_run_id and retained_resource_run_id below are NOT deleted or rewritten -- they remain the real, governed record of what was executed. Their status is amended to STALE_FEATURE_REPRESENTATION: neither authorizes a current CausalForest model, and neither may be reused to skip a corrected SMOKE/RESOURCE cycle once a representation is selected and this ADR's Feature-representation gate passes.",
    "unaffected_by_this_reopening": "honesty_support_algorithm, support_gate_correction, and synthetic_gate mechanics above are orchestration/diagnostic mechanics independent of feature representation and remain valid as declared."
  },
  "superseded_lifecycle_state": "T10_ACCEPTED",
  "lifecycle_state_evidence": {
    "note": "SMOKE and RESOURCE were both closed for T10's purposes under D31's final disposition (decision_record.final_disposition_locked_D31) BEFORE D32's feature-semantics reopening. Downstream seed-robustness (500K) and T11 FULL remain separate, not-yet-executed work governed by seed_robustness_plan_D31 / t11_full_and_stage4_policy_D31 above, and are additionally blocked by feature_semantics_reopening_event until a CausalForest representation is accepted.",
    "local_preflight_tests": "tests/test_causal_forest_baseline.py, 19/19 passed (post-support-gate-correction); STALE as of D32 -- the raw-representation synthetic fixtures are superseded by the D32 fixture rewrite (encoded generic numeric matrix + explicit raw-frame-rejection tests) that now also passes",
    "accepted_smoke_run_id": "t10_smoke_20260819T091713Z_996970 (STALE_FEATURE_REPRESENTATION per D32 -- raw f0-f11 fit directly, retained as historical record only)",
    "superseded_incomplete_local_run": "t10_smoke_20260819T074012Z_483118 (200,000 rows, old per-leaf gate, incomplete/failed -- retained as diagnostic evidence, not authoritative)",
    "retained_resource_run_id": "t10_resource_20260819T093301Z_574711 (2,000,000 rows, retained correctness/resource evidence, not rerun, not a seed-robustness stand-in; STALE_FEATURE_REPRESENTATION per D32 -- raw f0-f11 fit directly, retained as historical record only)"
  }
}
